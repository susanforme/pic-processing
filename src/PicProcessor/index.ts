export class PicProcessor {
  private config: PicProcessorConfig
  constructor(config: PicProcessorConfig) {
    this.config = config
  }
  // ...
}

/**
 * @description Configuration for the PicProcessor class
 */
export type PicProcessorConfig = {}
